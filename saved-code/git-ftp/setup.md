```bash
@rem config path will be merge to default user's ftp path
git config git-ftp.url "host"
git config git-ftp.remote-root "remote root"
git config git-ftp.user "user"
git config git-ftp.password "password"
```